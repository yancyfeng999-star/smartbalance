# 智余 · SmartBalance — 产品说明

| | |
|--|--|
| **中文名** | 智余 |
| **英文名** | SmartBalance |
| **一句话** | API 能查就查；不能查就读平台邮件；余额异常立刻通知 |
| **平台（V1）** | **macOS 15+** 菜单栏 |
| **形态** | 本机客户端 · 不建云账号 |
| **UI** | 视觉参考 [智额 SmartQuota](../智额/) |
| **状态** | V1 代码完成 · 待真机手工验收 |

与 **智额** 的分工：智额看**会员额度**；智余看 **API / TokenPlan 余额**，并做**主动报警**。

---

## 核心架构

```text
┌─────────────────────────────────────────────────────────┐
│                        智余                              │
│                                                         │
│  ┌── 数据源（怎么知道余额）──────────────────────────┐  │
│  │  ① API 直查（大多数平台）                          │  │
│  │  ② 平台固定邮件入站（不支持实时查询的平台）        │  │
│  └────────────────────────────────────────────────────┘  │
│                         ↓ 统一余额卡片                   │
│  ┌── 报警通道（怎么通知你）──────────────────────────┐  │
│  │  A. macOS 系统通知                                 │  │
│  │  B. 邮件报警（SMTP 发出）                          │  │
│  └────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 数据源 ① · API 直查（主路径）

- 大多数平台：用 **API Key / Base URL + Token** 直接调余额接口
- 定时或手动刷新 → 卡片展示金额 / 状态色
- 密钥存 **Keychain**

首批：`DeepSeek`、`New-API 兼容中转`、`OpenRouter`（可持续扩展）

### 数据源 ② · 平台固定邮件（辅路径）

- 部分平台**不提供实时余额 API**，只通过**固定发件邮箱**给你发「余额/用量/欠费」邮件
- 智余用 **IMAP** 登录你的收件箱，按规则匹配：
  - 发件人包含（如 `noreply@xxx.com`）
  - 主题包含（可选）
  - 正文正则提取金额
- 匹配成功 → 更新对应余额卡片（来源标记为「平台邮件」）
- 也支持**粘贴邮件正文试解析**（调试用）

### 报警通道 A · Mac 通知

- 余额进入 **偏低 / 危急 / 耗尽** 时发 **macOS 用户通知**
- 收到匹配的平台报警邮件时也可推送
- 需用户授权通知权限；可在设置中开关

### 报警通道 B · 邮件报警

- 同上触发条件，通过 **SMTP** 发到你配置的收件人
- 支持冷却时间，避免刷屏
- 支持测试邮件

两种报警通道**可同时开启**，彼此独立开关。

---

## 设置信息架构

1. **工作模式**  
   - 启用 API 查询  
   - 启用平台邮件读取（IMAP）
2. **API 账号** — 添加 DeepSeek / New-API 等  
3. **平台邮件源** — 发件人/主题/正则/阈值  
4. **收件箱 IMAP** — 主机、端口、账号、密码（读平台邮件用）  
5. **报警**  
   - ☑ Mac 通知  
   - ☑ 邮件报警（SMTP：主机、端口、发件/收件、阈值、冷却）  
6. **刷新间隔**

---

## 范围

### 做（V1）

| 能力 | 说明 |
|------|------|
| API 直查 | Provider 插件；DeepSeek + New-API |
| 平台邮件 | IMAP 拉取 + 规则匹配 + 金额解析 |
| Mac 通知 | UserNotifications |
| 邮件报警 | SMTP 发出 + 测试信 |
| 统一卡片 | 标注数据来源：API / 平台邮件 |
| 密钥 | Keychain |
| 视觉 | 对齐智额深色卡与状态色 |

### 不做（V1）

- Windows / Android  
- 云端同步、代理充值  
- 完整邮件客户端  
- 静默强更  

---

## 成功标准

实现路径已落地（Provider / IMAP / 通知 / SMTP / Keychain）；**带真密钥的 E2E 见 [PROJECT_STATUS.md](./PROJECT_STATUS.md)**。

- [x] API 账号可成功查余额并展示（DeepSeek / New-API / OpenRouter 代码路径 + 单测；真 key 手测 #1–3）  
- [x] 配置 IMAP + 规则后，能从平台邮件更新余额卡（解析/编排/试解析；真邮箱手测 #4–5）  
- [x] 阈值触发时：**Mac 通知**与/或**报警邮件**按开关送达（权限 UX + 冷却策略；真通道手测 #6–8）  
- [x] 密钥不落明文日志、不上传（Keychain；settings.json 仅 ref；手测 #10）

---

## 品牌

| 键 | 值 |
|----|-----|
| 中文名 | 智余 |
| 英文名 | SmartBalance |
| 副标题 | API 查询 · 平台邮件 · 双通道报警 |
| Bundle ID | `com.smartbalance.app` |
