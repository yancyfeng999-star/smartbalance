# 智余 · 产品说明

## 一句话

查各平台 **API / Token 余额**；金额偏低时用 **Mac 通知** 和 **SMTP 邮件** 提醒你。

弹层固定 **380×580**，主页与设置同尺寸、中间滚动。

---

## 能力

### 余额

1. **API 直查** — 各平台 Key / 系统令牌 / AK·SK
2. **控制台 Cookie** — MiMo / MiniMax / **apinebula** 可从 Chrome 一键导入登录态自动查余额
3. **系统令牌** — New-API / DMXAPI 等仍可贴令牌 + 用户 ID

### 用量统计

- 按本地自然天、ISO 自然周（周一开始）和自然月查看各渠道消费金额，可回看最多 400 天
- 有累计用量字段的渠道显示“接口统计”；其余渠道根据两次成功刷新之间的余额下降显示“余额估算”
- CNY、USD 和其他单位分别展示，不换算、不跨币种合计
- 数据仅保存在本机 `usage-history.json`，不含 API Key、Cookie、Access Token 或 Provider 响应正文

首次刷新只建立基线，不补算此前消费。充值、累计值重置、密钥或用户 ID 变化、统计方式变化不会记为消费；应用跨日未刷新时，差额归到后一次刷新所在日期，并在页面提示。

### 报警

- **Mac 通知**：系统通知中心
- **邮件报警**：SMTP 465 TLS 发到你的邮箱；支持冷却，避免刷屏

触发条件：余额状态为 **偏低 / 危急 / 耗尽**（按金额或百分比阈值）。

### 设置页结构

1. API 账号（按平台嵌套卡片）
2. 报警通知（Mac + SMTP）
3. 后台同步（间隔、阈值、登录启动、日志、更新、关于）

首页右上角齿轮进入设置，底栏“用量”进入统计页；三页共用固定 `380×580` 外壳。

---

## 明确不做

- Windows / Android
- 云账号 / 云同步
- 读供应商邮件（IMAP）
- 官方公证分发流水线（当前本地 ad-hoc 签名即可自用）

## 开源与隐私边界

- 源代码按根目录 Apache License 2.0（Apache-2.0）发布；贡献、漏洞报告和第三方归属分别遵循仓库公开文档。
- 余额、用量历史和设置默认只保存在本机；凭据只进入普通 macOS Keychain。
- 不默认上传遥测、崩溃、诊断或云同步数据；诊断/备份能力必须先完成脱敏和排除字段设计。
- Provider logo、服务名称和控制台链接只用于识别，不表示服务商背书；资源来源和许可证必须在开源发布前可追溯。

---

## 成功标准

1. 配置支持平台的密钥后，刷新能显示金额或清晰错误
2. 阈值触发时通知 / 邮件可用
3. 主页 ↔ 用量 ↔ 设置窗口不跳尺寸
4. 密钥只存在本机 Keychain（普通条目，不启用指纹/密码门禁），不进 settings.json
